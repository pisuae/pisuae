# Project Summary
The PIS UAE Marketplace is an innovative web application that enables tech enthusiasts to buy and sell a diverse range of tech components and electronics, including new, refurbished, and used items. It offers a user-friendly interface for product exploration and competitive pricing. Vendors can sell on a commission basis, enhancing the marketplace's offerings. The platform also includes a vendor analytics section that provides insights into sales performance and visitor engagement, aiding vendor decision-making.

# Project Module Description
The project comprises two main modules: **Frontend** and **Backend**.

## Frontend
- **Pages**: Includes product listings, product details, shopping cart, user orders, vendor signup/login, vendor dashboard, and a vendor analytics page.
- **Components**: Features reusable UI elements such as buttons, cards, headers, and forms, including an image upload component for vendor product listings.
- **State Management**: Utilizes React hooks for effective state and side effect management.

## Backend
- **API Endpoints**: Handles CRUD operations for products, orders, vendors, and cart items.
- **Database Models**: Defines schemas for products, orders, user authentication, vendor profiles, and product views.

# Directory Tree
```
app/
├── backend/
│   ├── README.md
│   ├── alembic/
│   ├── core/
│   ├── data_models/
│   ├── dependencies/
│   ├── lambda_handler.py
│   ├── main.py
│   ├── middlewares/
│   ├── models/
│   ├── requirements.txt
│   └── routers/
└── frontend/
    ├── README.md
    ├── public/
    ├── src/
    │   ├── components/
    │   │   ├── Header.tsx
    │   │   ├── ProductCard.tsx
    │   │   ├── ImageUpload.tsx
    │   ├── pages/
    │   │   ├── Index.tsx
    │   │   ├── ProductDetail.tsx
    │   │   ├── VendorDashboard.tsx
    │   │   └── VendorAnalytics.tsx
    │   ├── tailwind.config.ts
    │   ├── tsconfig.json
    │   ├── vite.config.ts
    │   └── index.html
    └── site.config.json
```

# File Description Inventory
- **backend/**: Contains the backend application code, including API routes, models, and database migrations.
- **frontend/**: Contains the frontend application code, including pages, components, and styles.
- **README.md**: Documentation for both frontend and backend.
- **todo.md**: Development tasks and design guidelines for vendor features.
- **image.ts**: Utility for resolving and uploading product images.
- **VendorAnalytics.tsx**: Enhanced page for displaying vendor sales analytics with new chart types, metrics, and data export functionality.
- **index.html**: Main HTML file for the frontend application.
- **site.config.json**: Configuration file for the frontend, now includes Google Analytics 4 measurement ID.

# Technology Stack
- **Frontend**: React, TypeScript, Vite, Tailwind CSS, Recharts
- **Backend**: Python, FastAPI, SQLAlchemy, Alembic
- **Database**: PostgreSQL

# Usage
1. **Install Dependencies**:
   - For the backend: Navigate to `app/backend` and run `pip install -r requirements.txt`.
   - For the frontend: Navigate to `app/frontend` and run `pnpm install`.
   
2. **Build**:
   - For the frontend: Run `pnpm run build` in the `app/frontend` directory.

3. **Run**:
   - Start the backend server by executing `python main.py` in the `app/backend` directory.
   - Start the frontend application using `pnpm run dev` in the `app/frontend` directory.
