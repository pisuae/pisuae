# Project Summary
The PIS UAE Marketplace is a comprehensive web application designed for tech enthusiasts to buy and sell a diverse range of tech components and electronics, including new, refurbished, and used items. It features a user-friendly interface for seamless product exploration and competitive pricing. Vendors can list their products on a commission basis, and the platform includes analytics tools for vendors to track performance and engagement. Recent enhancements include a product review and rating system, a date range picker for spending trends, and the addition of a "Phones" category to accommodate the latest mobile devices.

# Project Module Description
The project consists of two primary modules: **Frontend** and **Backend**.

## Frontend
- **Pages**: Includes product listings, product details with reviews, shopping cart, user orders, vendor signup/login, vendor dashboard, analytics, and a new "Phones" category.
- **Components**: Reusable UI elements including buttons, cards, headers, forms, and a star rating system for product reviews.
- **State Management**: Utilizes React hooks for effective state management.

## Backend
- **API Endpoints**: Handles CRUD operations for products, orders, vendors, and reviews.
- **Database Models**: Defines schemas for products, orders, user authentication, vendor profiles, and reviews.

# Directory Tree
```
app/
├── backend/
│   ├── README.md
│   ├── alembic/
│   ├── core/
│   ├── data_models/
│   ├── dependencies/
│   ├── lambda_handler.py
│   ├── main.py
│   ├── middlewares/
│   ├── models/
│   ├── requirements.txt
│   ├── routers/
│   ├── services/
│   └── mock_data/
│       ├── products.json
│       └── reviews.json
└── frontend/
    ├── README.md
    ├── public/
    ├── src/
    │   ├── components/
    │   │   ├── Header.tsx
    │   │   ├── ProductCard.tsx
    │   │   ├── ImageUpload.tsx
    │   │   ├── ui/
    │   │   │   ├── calendar.tsx
    │   │   │   └── popover.tsx
    │   ├── pages/
    │   │   ├── Index.tsx
    │   │   ├── ProductDetail.tsx
    │   │   ├── Products.tsx
    │   │   ├── VendorDashboard.tsx
    │   │   ├── VendorAnalytics.tsx
    │   │   └── Profile.tsx
    │   ├── tailwind.config.ts
    │   ├── tsconfig.json
    │   ├── vite.config.ts
    │   └── index.html
    └── site.config.json
```

# File Description Inventory
- **backend/**: Contains the backend application code, including API routes, models, services, and database migrations.
- **frontend/**: Contains the frontend application code, including pages, components, and styles.
- **README.md**: Documentation for both frontend and backend.
- **todo.md**: Development tasks and design guidelines for vendor features.
- **image.ts**: Utility for resolving and uploading product images.
- **ProductCard.tsx**: Displays product information with average ratings and a review count.
- **ProductDetail.tsx**: Shows detailed product information along with customer reviews and a review submission form.
- **calendar.tsx**: Component for the date range picker functionality.
- **popover.tsx**: Component for the popover used in the date range picker.
- **index.html**: Main HTML file for the frontend application.
- **site.config.json**: Configuration file for the frontend, now includes Google Analytics 4 measurement ID.

# Technology Stack
- **Frontend**: React, TypeScript, Vite, Tailwind CSS, Recharts, date-fns, react-day-picker
- **Backend**: Python, FastAPI, SQLAlchemy, Alembic
- **Database**: PostgreSQL

# Usage
1. **Install Dependencies**:
   - For the backend: Navigate to `app/backend` and run `pip install -r requirements.txt`.
   - For the frontend: Navigate to `app/frontend` and run `pnpm install`.
   
2. **Build**:
   - For the frontend: Run `pnpm run build` in the `app/frontend` directory.

3. **Run**:
   - Start the backend server by executing `python main.py` in the `app/backend` directory.
   - Start the frontend application using `pnpm run dev` in the `app/frontend` directory.
